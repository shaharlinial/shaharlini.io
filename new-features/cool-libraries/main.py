class Library:
    def __init__(self, name, version, description, url:
            self.name = name
            self.version = version
            self.description = description
            self.url = url
